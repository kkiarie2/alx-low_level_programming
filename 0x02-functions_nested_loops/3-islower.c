#include "main.h"



/**
 * _islower - checks if char is lower
 * @c: character to check
 * Return: prints 1 if char is lower and 0 if not
 */

int _islower(int c)
{

return (c >= 'a' && c <= 'z');

}
